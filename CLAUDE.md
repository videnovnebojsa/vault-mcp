# Claude Guidance

Use the HTTP MCP server endpoint from `.mcp.json` or the stdio bridge entrypoint documented in `package.json`.

The old `--alias` command-line option is not supported. Configure vault names through environment configuration instead of passing aliases on the command line.
